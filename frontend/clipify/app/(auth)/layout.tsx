import React from "react";

/** Auth routes: full-page flows without public marketing shell (no TopNav). */
export default function AuthRouteLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
