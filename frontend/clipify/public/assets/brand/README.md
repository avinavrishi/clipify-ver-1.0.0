# Brand / logos

App and brand logos (e.g. Clipify logo). Reference via `getBrandLogoUrl('filename.ext')` from `lib/assets.ts`.

To serve from server: set `NEXT_PUBLIC_ASSETS_BASE_URL` to your assets base URL.
